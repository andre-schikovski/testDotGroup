{
  "name": "auto_saucedemo",
  "version": "1.0.0",
  "description": "Automacao de testes para portal SauceDemo usando Cypress",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:e2e": "cypress run --spec cypress/e2e/e2e.cy.js --env allure=true && allure generate allure-results --clean -o allure-report && allure open"
  },
  "author": "Andre Luiz Schikovski",
  "license": "ISC",
  "dependencies": {
    "cypress": "^14.3.0"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.41.0"
  }

  
}
